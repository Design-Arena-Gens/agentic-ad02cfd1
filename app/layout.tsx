import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  metadataBase: new URL("https://agentic-ad02cfd1.vercel.app"),
  title: {
    default: "Atelier Supper Club — QR Menu",
    template: "%s | Atelier Supper Club",
  },
  description:
    "Explore the Atelier Supper Club tasting menu. Scan each QR code to reveal tasting notes, ingredients, and sommelier pairings for every dish.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
